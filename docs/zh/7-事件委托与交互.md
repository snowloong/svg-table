# 背景

SVG 表格组件涉及大量交互（如点击、悬停、拖拽等），为提升性能和可维护性，采用事件委托和热点区域管理策略，减少事件监听器数量。

---

## 实现细节
- 将鼠标、点击等事件绑定在父 <g> 或 SVG 根节点，通过事件冒泡判断目标。
- 只为可交互区域绑定事件，静态装饰性节点不绑定事件。
- 支持批量选中、右键菜单、拖拽排序等高级交互。
- 事件处理与节点池、分层分组协同。

---

## 待解决问题
- 事件委托下的目标识别与性能瓶颈。
- 热点区域的动态变化与事件绑定同步。
- 高并发交互下的事件冲突与丢失。
- 事件与动画、批量更新的协同。 